FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn

COPY agents_mock_app.py /app/agents_mock_app.py

EXPOSE 8010

CMD ["uvicorn", "agents_mock_app:app", "--host", "0.0.0.0", "--port", "8010"]
